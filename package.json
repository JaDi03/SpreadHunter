{
  "name": "spreadhunter",
  "version": "1.0.0",
  "description": "Cross-DEX EURC/USDC arbitrage agent on Arc Testnet",
  "private": true,
  "scripts": {
    "backend": "cd backend && node src/index.js",
    "frontend": "cd frontend && pnpm dev",
    "register": "cd backend && node scripts/registerAgent.mjs",
    "dev": "echo Please run backend and frontend separately in two terminals."
  }
}
